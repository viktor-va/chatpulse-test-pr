server {
  listen 80;
  server_name chatpulse.localhost;
  root /var/www/html/public;

  index index.php index.html;

  access_log /var/log/nginx/access.json.log json_combined;
  error_log  /var/log/nginx/error.log;

  location / {
    try_files $uri $uri/ /index.php?$query_string;
  }

  location ~ ^/api/chat/(.*)$ {
    set $upstream_uri /api/$1;
    include fastcgi_params;

    fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
    fastcgi_param SCRIPT_NAME     /index.php;
    fastcgi_param PATH_INFO       "";

    fastcgi_param REQUEST_URI     $upstream_uri;
    fastcgi_param QUERY_STRING    $query_string;

    fastcgi_param HTTP_X_INTERNAL_GATEWAY_SECRET super-long-random-string-here;

    fastcgi_pass chat-api:9000;
  }

  location ~ \.php$ {
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    fastcgi_pass php:9000;
    fastcgi_read_timeout 120s;
  }

  client_max_body_size 20m;
  sendfile on;
  gzip on;

  # WebSocket proxy (strip /ws prefix)
  location /ws/ {
    proxy_pass http://reverb:8081/;   # trailing slash is IMPORTANT
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_read_timeout 300s;
  }

  # Optional: normalize /ws → /ws/
  location = /ws {
    return 301 /ws/;
  }
}

